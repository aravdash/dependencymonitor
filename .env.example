# Used by Docker Compose. For host JVMs export variables in the service's shell.
GITHUB_TOKEN=
ALERT_WEBHOOK_URL=
ALERT_SEVERITY_THRESHOLD=critical
POSTGRES_DB=dependency_health
POSTGRES_USER=health
POSTGRES_PASSWORD=health
